{
  "name": "gesturemed-workspace",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:backend": "npm --prefix medisign-voice/backend run dev",
    "dev:frontend": "npm --prefix medisign-voice/frontend run dev",
    "build": "npm --prefix medisign-voice/frontend run build",
    "seed": "npm --prefix medisign-voice/backend run seed"
  }
}
